server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location = /display {
    return 301 /display/;
  }

  location /display/ {
    try_files $uri $uri/ /display/index.html;
  }
}
